const ALLOWED_TIMEFRAMES = ['15m', '1h', '4h', '1d'];
const DEFAULT_EXCHANGE = 'binance';
const DEFAULT_QUOTE = 'USDT';

module.exports = {
  ALLOWED_TIMEFRAMES,
  DEFAULT_EXCHANGE,
  DEFAULT_QUOTE,
};
